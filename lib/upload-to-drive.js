// this is the module for uploading certificates
// middleware for uploading certificates to drive
const uploadToDrive = async (cert) => {
  // use fetch api to post certificates
}

module.exports = { uploadToDrive };